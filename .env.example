# Public origin of the app. Used for absolute URLs and OAuth callbacks.
APP_URL=http://localhost:3000

# GitHub personal access token used to read the dashboard's repositories.
# Needs `repo` for private repositories, `public_repo` is enough otherwise.
GITHUB_TOKEN=

# Connection string for the task store, once one is wired up.
DATABASE_URL=
